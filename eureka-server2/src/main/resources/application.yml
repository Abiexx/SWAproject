server:
  port: 8762

eureka:
   instance:
     hostname: EurekaServer2
     appname: Eureka-cluster
   environment: local
   client:
      serviceUrl:
        defaultZone:
                http://EurekaServer1:8761/eureka
      register-with-eureka: true
      fetch-registry: true



